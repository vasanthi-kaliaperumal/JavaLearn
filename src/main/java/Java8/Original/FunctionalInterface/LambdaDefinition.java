package Java8.Original.FunctionalInterface;

public class LambdaDefinition {
    public static void main(String ar[]){
        /*
        1. Lambda is a nameless function
        2. Can be invoked/called/used only with Functional Interface
        3. Functional Interface reference is used to refer lambda expression
        //Example1
        () -> System.out.println("Lambda without parameter");

        //Example2
        num1 -> System.out.println("Lambda with parameter without return type");

        //Example3
        num1 -> {return (num1*num2);};

        Exercise:
        1. n -> return n*n; => invalid -> lambda with return keyword must be enclosed within braces
        2. n -> {return n*n;} => valid -> eventhough statement doesnot end with ; its valid
        3. n -> {return n*n}; => invalid -> return statement must end with ;
        4. n -> {return n*n} => invalid -> return statement must end with ;
        5. n -> {n*n;} => invalid ->
        6. n -> n*n; => valid ->
       .

        */
    }
}
